import AdminLayout from './AdminLayout';

export default function AdminIndexPage() {
  return (
    <AdminLayout>
      {/* Dashboard content will be rendered inside AdminLayout by default if no children passed */}
    </AdminLayout>
  );
}
